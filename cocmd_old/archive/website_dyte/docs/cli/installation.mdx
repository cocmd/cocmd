---
sidebar_position: 1
slug: /
---

# cocmd CLI

The cocmd CLI is a unified utility that helps you manage cocmd services for your
organization(s) directly from your command line.

## Installation

You can install the CLI from the NPM registry using `npm` or `yarn`.

```bash npm2yarn
npm install -g @dytesdk/cli
```
