---
image: /static/ui-kit/1.x.x/components/dyte-controlbar-button.svg
---

# cocmdControlbarButton

A skeleton component used for composing buttons.

```jsx
<Center>
  <cocmdControlbarButton
    label="Flight Mode"
    icon={airplaneSVG}
    onClick={() => alert('Flight mode toggled')}
    size="sm"
  />
  <cocmdControlbarButton
    label="Flight Mode"
    icon={airplaneSVG}
    onClick={() => alert('Flight mode toggled')}
    size="lg"
  />
  <cocmdControlbarButton
    label="Flight Mode"
    icon={airplaneSVG}
    onClick={() => alert('Flight mode toggled')}
    variant="horizontal"
    size="sm"
  />
</Center>
```

## Props

<PropsTable of="dyte-controlbar-button" />
