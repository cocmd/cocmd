# cocmdParticipantsToggle

A button which toggles visibility of participants.

When clicked it emits a `dyteStateUpdate` event with the data:

```ts
{
  activeSidebar: boolean;
  sidebar: 'participants' | 'none';
}
```

```jsx
<Center>
  <cocmdParticipantsToggle size="sm" />
  <cocmdParticipantsToggle size="lg" />
  <cocmdParticipantsToggle variant="horizontal" size="sm" />
</Center>
```

## Props

<PropsTable of="dyte-participants-toggle" />
