# cocmdMoreToggle

A button which toggles visibility of a more menu.

When clicked it emits a `dyteStateUpdate` event with the data:

```ts
{
  activeMoreMenu: boolean;
}
```

```jsx
<Center>
  <cocmdMoreToggle size="sm" />
  <cocmdMoreToggle size="lg" />
  <cocmdMoreToggle variant="horizontal" size="sm" />
</Center>
```

## Props

<PropsTable of="dyte-more-toggle" />
